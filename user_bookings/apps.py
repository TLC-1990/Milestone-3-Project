"""Configuration for the user_bookings app."""
from django.apps import AppConfig


class UserBookingsConfig(AppConfig):
    """Configuration for the user_bookings app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "user_bookings"
